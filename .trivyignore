# Trivy Ignore File
# Add CVE IDs here to suppress false positives or accepted risks
# Format: CVE-YYYY-NNNNN
#
# Before adding a CVE here, document:
# 1. Why this is a false positive OR
# 2. Why the risk is acceptable
# 3. When to revisit (if applicable)
#
# Example:
# CVE-2023-12345  # Not applicable: feature not used in our configuration

# step-ca v0.29.0 false positives
# CVE-2025-44005: Authorization Bypass in ACME and SCEP Provisioners
# Fixed in v0.29.0 (GHSA-h8cp-697h-8c8p states vulnerable versions <= 0.28.4)
# We use v0.29.0 which is the patched version - Trivy DB not yet updated
# Revisit: Remove once Trivy database correctly identifies v0.29.0 as fixed
CVE-2025-44005
